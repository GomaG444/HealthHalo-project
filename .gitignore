# ───── Runtime/session artefacts ──────────────────────────
# flask_session/

# ───── Environment / secrets ──────────────────────────────
.env
.env.* # e.g. .env.local, .env.prod
secrets.txt
key.env         # legacy

# ───── User-uploaded data & generated outputs ─────────────
uploads/
summaries/
*.json          # This will ignore any JSON files in the root folder, like your latest.json file.

# ───── Python build artefacts ─────────────────────────────
__pycache__/
*.pyc
*.pyo

# ───── Misc ───────────────────────────────────────────────
# (uncomment if you’re on macOS or VS Code)
.DS_Store
# .vscode/

# ───── Python virtual environment ─────────────────────────
venv/